#include<bits/stdc++.h>
using namespace std;
inline int read(){
	int num=0;char ch=getchar();
	for(;!isdigit(ch);ch=getchar()) ;
	for(;isdigit(ch);ch=getchar()) num=num*10+ch-'0';
	return num;
}
int type,n,m,k,q;
int main(){
	freopen("corridor.in","r",stdin);
	freopen("corridor.out","w",stdout);
	srand(time(0));
	type=read(),n=read(),m=read(),k=read();
	for(int i=1,x,y;i<=k;i++) x=read(),y=read();
	q=read();
	for(int i=1;i<=q;i++) printf("%d %d",rand()%(n-1)+1,rand()%(m-1)+1);
	return 0;
}