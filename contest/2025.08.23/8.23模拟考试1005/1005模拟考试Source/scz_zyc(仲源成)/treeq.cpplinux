#include <bits/stdc++.h>
using namespace std;
#define freall(file) \
		freopen(file".in","r",stdin);\
		freopen(file".out","w",stdout);
int n;
vector<int>a[10000000];
void dfs(int here,int father)
{
	
}
int main()
{
	freall("treeq");
	scanf("%d",&n);
	int t=n;
	int ans=0;
	while(--t)
	{
		int u,v,w;
		u=n-t+1;
		scanf("%d %d",&v,&w);
		a[u].push_back(v);
		a[v].push_back(u);
		ans=ans^w;
	}
	dfs(1,0);
	printf("%d",ans);
 	return 0;
}
