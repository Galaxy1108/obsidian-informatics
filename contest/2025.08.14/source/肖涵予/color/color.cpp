#include <bits/stdc++.h>
using namespace std;
const int N = 2005;
int n, a[N];
int main(void) {
	freopen("color.in", "r", stdin);
	freopen("color.out", "w", stdout);
	scanf("%d", &n);
	for (int i = 1; i <= n; ++i) scanf("%d", &a[i]);
	printf("4");
	return 0;
}
