#include <bits/stdc++.h>
using namespace std;
int n, m, s, t; 
int main(void) {
	freopen("rabbit.in", "r", stdin);
	freopen("rabbit.out", "w", stdout); 
	scanf("%d %d %d %d", &n, &m, &s, &t);
	for (int i = 1, x, y, o; i <= m; ++i) scanf("%d %d %d", &x, &y, &o);
	printf("0 0");
	return 0;
}
